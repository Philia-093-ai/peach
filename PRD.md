# PRD｜peach·AI 小工具站——第一个工具「中文名字生成器」（MVP）

- 版本：v2.0（Day 4，基于 Day 3 research.md 的方向修订）
- 状态：待技术选型（Day 5）
- 里程碑：Day 7 完成 MVP 上线
- 上游文档：`research.md`（Day 3 竞品调研与本期范围划定）

---

## 1. 项目概述

**一句话**：一个面向普通中文用户的 AI 小工具站，本期只做 **1 个工具——中文名字生成器**：用户输入场景和关键词，AI 生成一批好听、有寓意的名字。

- **目标用户**：想给品牌/店铺/宠物/昵称起名的普通中文用户，不注册、打开就用。
- **解决的问题**：起名靠拍脑袋和翻字典，费时且没灵感；需要一个「输入想法 → 立刻拿到一批带解释的名字」的轻工具。
- **本期定位**：单点验证（research.md 结论：一个功能做到「真的好用」就能立足，聚合是后话）。
- **北极星体验**：**新用户不注册、不学习，30 秒内拿到第一次结果。**

## 2. 范围与取舍（本期做什么、砍掉什么）

### 2.1 本期做（MVP，3 个功能）

| 功能 | 一句话描述 |
| --- | --- |
| 名字生成（核心） | 用户选择命名场景、输入关键词，点击生成，得到 ≥ 5 个名字，每个附寓意解释 |
| 工具首页 | 站点标题 + 一句话说明 + 生成表单 + 结果展示区，一人一页完成全流程 |
| 每日免费额度 | 每个访客每天免费生成 5 次，用完明确提示「明日再来」 |

### 2.2 本期砍掉（承接 research.md「本期不做」清单，逐项给理由）

| 砍掉的功能 | 砍它的理由 | 属于哪类理由 |
| --- | --- | --- |
| 多工具聚合站（起名、文案、抠图都做） | Copy.ai 教训：什么都做会把定位做丢；Remove.bg 证明单点可以立足。本期先验证 1 个工具有没有人用 | ② 核心体验不依赖它 |
| 用户注册 / 登录 | 「打开就用」是 30 秒北极星体验的前提，账号体系直接把用户拦在门外 | ② 核心体验不依赖它 |
| 会员 / 支付系统 | 价值未验证，先免费跑通；每日限额已能控制成本 | ② 核心体验不依赖它 |
| 移动 App / 小程序 | 网页浏览器能打开就满足 MVP，多端是验证后的扩张动作 | ① 时间盒装不下 |
| 多语言支持 | 目标用户是中文用户，只做中文 | ② 核心体验不依赖它 |
| API 对外开放 | Remove.bg 的 API 是成熟期业务，不是起步的事 | ① 时间盒装不下 |
| 历史记录 / 云端存储 | 结果当场看、当场复制即可；历史记录是锦上添花 | ③ 有更轻的替代（当场复制） |
| 精细 UI 动效 | 好用优先，好看其次 | ③ 有更轻的替代（简洁静态界面） |
| 名字收藏 / 打分 / 二次编辑 | 依赖历史记录与账号，属验证后的增强体验 | ② 核心体验不依赖它 |

> 规则（沿用 research.md）：以上任何一条，未经理由充分的重新评估，Day 4–Day 7 不碰。

## 3. 功能需求

### 3.1 名字生成（核心功能）

- **描述**：用户提交「场景 + 关键词」，系统返回一批名字及寓意解释。
- **用户流程**：
  1. 用户在首页选择命名场景（下拉：品牌 / 店铺 / 宠物 / 网络昵称，默认「品牌」）；
  2. 输入关键词（1–20 字，描述想表达的意思、风格或行业，如「海边咖啡店，安静治愈」）；
  3. 点击「生成」按钮，按钮进入加载态；
  4. **10 秒内**展示结果：≥ 5 个名字，以卡片列出，每张卡片含「名字 + 一句话寓意（30 字内）」；
  5. 每张卡片有「复制」按钮，一键复制名字文本。
- **边界情况**：
  - 关键词为空 / 超过 20 字：提交被拒绝，表单旁给出明确提示，不发起请求；
  - 生成失败或超时（> 10 秒）：显示「生成失败，请重试」提示，页面不出现报错堆栈，可重新点击生成；
  - 结果中的名字按纯文本展示，页面样式不被注入内容破坏。

### 3.2 工具首页

- **描述**：唯一页面，包含站点标题、一句话工具说明、生成表单、结果展示区。
- **用户流程**：
  1. 用户打开网站，无需任何注册、引导或学习，直接看到表单；
  2. 完成一次生成后，结果出现在表单下方，可再次修改输入、继续生成（受每日额度限制）；
  3. 页面底部注明「每日免费生成 5 次」。
- **边界情况**：
  - 首次打开时结果区显示简短示例（「例如：海边咖啡店 → 『浪屿』…」），帮助用户理解输入格式，不设新手教程。

### 3.3 每日免费额度

- **描述**：每个访客每天可免费生成 5 次，次数用尽后当天不能再生成。
- **用户流程**：
  1. 每次成功生成消耗 1 次额度；
  2. 第 6 次点击生成时，提示「今日免费次数已用完，明天再来吧」，不发起请求；
  3. 次日额度自动恢复，无需任何操作。
- **边界情况**：
  - 生成失败/超时的那次**不消耗**额度；
  - 计数方式（如浏览器本地计数）由 Day 5 技术选型决定，MVP 不追求防刷，只求「明确告知限额」。

## 4. 验收标准（MVP 完成的定义，逐条可检验）

- [ ] A1 新访客从打开首页到看到第一批生成结果，全程不注册、无引导步骤，总耗时 ≤ 30 秒。
- [ ] A2 首页展示站点标题、一句话说明和生成表单，无新手教程也能看懂怎么用。
- [ ] A3 选择场景、输入关键词后点击「生成」，10 秒内返回结果。
- [ ] A4 每次生成返回 ≥ 5 个名字，每个名字附 ≤ 30 字的寓意解释，且有「复制」按钮。
- [ ] A5 关键词为空时点击生成被拒绝，并显示明确提示，不发起请求。
- [ ] A6 关键词超过 20 字时点击生成被拒绝，并显示明确提示。
- [ ] A7 当日第 6 次生成被拒绝，显示「今日次数已用完」提示；次日可正常使用。
- [ ] A8 生成失败或超时（> 10 秒）时显示重试提示，无报错堆栈，且该次不消耗额度。
- [ ] A9 部署后，通过公开链接在手机和电脑浏览器上均可完成 A1–A8（无横向滚动、按钮可点）。
- [ ] A10 结果中的名字与寓意为中文，页面样式不被生成内容破坏。

## 5. 非目标与后续迭代（砍掉的功能去哪儿了）

按验证后的优先级排序：

1. **v1.1 候选**：第二个小工具（聚合站起步，验证「工具站」模式）；
2. **v1.2 候选**：名字收藏 / 历史记录、结果打分反馈（用于改进生成质量）；
3. **v2.0 候选**：注册登录与账号体系、会员支付、多语言、移动 App / 小程序、对外开放 API、精细 UI 动效。

## 6. 术语与假设

- **术语**：
  - 「一次生成」：用户点击生成按钮并成功返回一批结果，计 1 次额度；
  - 「场景」：名字的用途分类（品牌 / 店铺 / 宠物 / 网络昵称）；
  - 「关键词」：用户输入的期望含义、风格或行业描述。
- **假设**：
  - 依赖一个文本生成模型 API 完成 AI 生成（具体选型 Day 5 决定，PRD 只约定输入输出与时效）；
  - 「名字好听与否」是主观体验，MVP 验收只要求结果可用、可读、相关（与关键词和场景相关）；
  - 单访客量级为早期验证量级，每日限额足以控制 API 成本，不做防刷体系；
  - 内容安全由所使用的模型服务端策略兜底，MVP 不自建审核。
