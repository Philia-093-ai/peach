# AGENTS.md — 项目协作规则

> 本文件是给 AI 助手（如 WorkBuddy / Codex / Claude Code 等）看的项目规则。
> 任何 AI 在这个文件夹里干活之前，先读完本文件。

## 项目概况

- **项目名称**：my-project（Day 1 暂定名，可后续修改）
- **当前阶段**：Day 1 — 环境搭建，还没有代码
- **计划**：Day 2 开始写代码、创建 GitHub 仓库

## 基本规则

1. **语言**：与用户交流使用简体中文；代码注释和提交信息可用中文或英文，保持一致即可。
2. **改动前先说明**：修改或删除已有文件前，先告诉用户要做什么、为什么。
3. **不做危险操作**：不执行 `rm -rf`、批量删除、修改系统配置等操作，除非用户明确确认。
4. **小步提交**：代码写好后分小块给用户看，不要一次性堆一大坨。
5. **不懂就问**：需求模糊时先确认，不要自作主张猜方向。

## 追加规则（2026-09-24 · Day 6）

> 以下规则按「只追加、不重写」原则补充，上面 Day 1 的规则全部保留。

6. **当天只做当天任务**：每天只做当天任务清单上的事；学员没说「进入下一板块」，就不越界去做后面的内容。
   - 说明：这条是 Day 1 的标准规则，Day 6 检查时发现当时漏写入，现补录。
   - **防的是**：AI 或学员顺手把后面几天的活提前干掉，学习节奏被打乱。
7. **开工先验仓库**（我的规则）：AI 每天开工的第一步，先核对本地仓库与远程一致——`git status` 干净、本地 main 与 origin/main 同步、`.git` 目录完好；发现异常立即停下报告，确认后再开始当天任务。
   - **防的是**：Day 4 真实发生过的 `.git` 目录莫名消失事故——开工先验，损失为零；干完活才发现，可能白干一整天。

## 文件约定

- 源代码放在本文件夹根目录或 `src/` 下（Day 2 确定结构）。
- 临时文件、草稿放 `tmp/`（可随时删除）。
- 文档放 `docs/`（如果以后需要）。

## Git / GitHub 约定（Day 2 生效）

- GitHub 用户名：（注册完成后填在这里）
- 提交信息格式：`类型: 简短描述`，例如 `feat: 添加首页`、`fix: 修复按钮点击`。
- 只提交有意义的文件，不提交临时文件。

## 变更记录

- 2026-09-18：Day 1 创建本文件，完成环境搭建。
- 2026-09-24：Day 6 本文件首次入库（GitHub: Philia-093-ai/peach）。检查 Day 1 规则，5 条基本规则完好；按完成标准补录漏写的「当天只做当天任务」；追加我的规则「开工先验仓库」。项目实际已推进至 Day 6（仓库即本项目）。
